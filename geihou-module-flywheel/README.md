# geihou-module-flywheel

Status: empty flywheel module JAR baseline.

Package root: `com.geihou.module.flywheel`

Reserved port: `48089`

This module belongs to the standalone Geihou aggregator only and is not runtime-ready.

It imports the Geihou BOM for dependency management, but has no direct dependencies.

Rules:

- Do not migrate dashboard, task, metric, loop, growth, automation, review, or learning flywheel code in this baseline slice.
- Do not add Java implementation or runtime configuration.
- Do not treat this baseline as a running flywheel service.
