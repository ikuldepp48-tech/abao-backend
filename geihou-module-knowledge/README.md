# geihou-module-knowledge

Status: empty knowledge module JAR baseline.

Package root: `com.geihou.module.knowledge`

Reserved port: `48091`

This module belongs to the standalone Geihou aggregator only and is not runtime-ready.

It imports the Geihou BOM for dependency management, but has no direct dependencies.

Rules:

- Do not migrate revisit, knowledge base, wiki, document, recommendation, RAG, embedding, or refinement code in this baseline slice.
- Do not add Java implementation or runtime configuration.
- Do not treat this baseline as a running knowledge service.
